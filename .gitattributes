# BCOS는 Windows와 POSIX 환경에서 동시에 개발된다.
# 저장소 안의 개행은 항상 LF로 통일한다. CRLF가 섞이면 diff가 오염되고
# Codex의 구현 diff를 Claude가 리뷰할 수 없게 된다.
* text=auto eol=lf

# append-only 감사 로그. 머지는 라인 단위로 두 쪽을 모두 보존해야 한다.
.bcos/events.jsonl merge=union

# 파생 인덱스. 충돌 시 손으로 풀지 말고 `bcos reindex`로 재생성한다.
.bcos/state.json -merge
